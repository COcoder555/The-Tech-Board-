<div>
     <h3>Post Title: {{title}}</h3>
     <h4>User: {{user.userName}}</h4>
     <p>{{content}}</p>
     <p2>{{createdAt}}</p2>
     {{#each comments as |comment|}}
<div>
     <h3>Comments:</h3>
     <h4> User: {{comment.user.userName}}</h4>
     <p>
          {{comment.content}}
     </p>

</div>
{{/each}}
</div>
  
 
<div class="col-md-6">


    <form class="form new-Post-form">
     

     
    </div>
    
        <label for="content">Leave a Comment:<span id = "postID"> {{id}} </span></label>
        <textarea class="form-input" id="content" name="content"></textarea>
      </div>
      <div class="form-group">
        <button type="submit" id = "submit"  class="btn btn-primary">Create</button>
             {{!-- <div class="form-group">
           <div class="col-md-4">
        <button class="btn btn-sm btn-danger commentDelete" data-id="{{comment.id}}">DELETE</button>
        <button class="btn btn-sm btn-danger commentUpdate" data-id="{{comment.id}}">UPDATE</button>
       
      {{!-- </div> -- --}}
      </div>
      </form>
   </div>
  
  </div>

  <script src = '/js/comment.js'></script>